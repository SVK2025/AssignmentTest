{
  // Logging configuration
  "Logging": {
    "LogLevel": {
      // Default logging level for all categories
      "Default": "Information",
      // Override logging level for Microsoft.AspNetCore category
    "Microsoft.AspNetCore": "Warning"
    }
  },
  // Configure which hosts can access the application
  "AllowedHosts": "*"
}
